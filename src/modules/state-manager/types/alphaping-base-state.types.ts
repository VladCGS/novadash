export const STATE_EVENTS = {
  STATE_UPDATED: 'updated',
  STATE_CREATED: 'created',
  STATE_LOADED: 'loaded',
  STATE_WRITTEN: 'written',
};

export enum InitStateEvents {
  ERROR = 'error',
}

export enum AppStatusesBaseEnum {
  DONE = 'DONE',
  PROCESSING = 'PROCESSING',
  ERROR = 'ERROR',
}
